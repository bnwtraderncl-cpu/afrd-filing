[pytest]
testpaths = tests
# tests/ is on sys.path so the suite can import its own helpers, and the
# package is imported from the repo root rather than from an install, so
# test_read_only.py scans THIS working tree.
pythonpath = . tests
